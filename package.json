{
  "name": "gbit-db",
  "version": "1.0.0",
  "description": "CLI que gera um backend completo Next.js + Prisma + Database GBIT + GBIT Container (substitui o Docker) + Portal com IA de código.",
  "type": "module",
  "bin": {
    "gbit-db": "bin/gbit-db.js"
  },
  "files": [
    "bin",
    "lib",
    "templates"
  ],
  "scripts": {
    "start": "node bin/gbit-db.js",
    "build": "node -e \"console.log('✓ gbit-db CLI pronto para distribuição')\"",
    "test": "node --test",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "chalk": "^5.3.0",
    "gbit-db-dados": "file:templates/gbit-db-dados",
    "jsonwebtoken": "^9.0.3",
    "tailwind-merge": "^3.6.0"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "Gislaine-programadora",
  "license": "MIT",
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.10"
  }
}
